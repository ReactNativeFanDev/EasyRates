import {RFValue} from 'react-native-responsive-fontsize';

export const appColors = {
  background: '#151718',
  primary: '#08d65c',
  secondary: '#595c5b',
};

export const size = (props: number) => RFValue(props, 580);
